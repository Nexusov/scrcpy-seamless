# Seamless 2.0 risk register

Phase 0 inventory, 2026-09-23. These are observed gaps or explicitly identified
future risks, not claims that an exploit or crash has been reproduced.
Evidence is mapped in the [1.x baseline](SEAMLESS_1_BASELINE.md); intended changes
are in the [target](SEAMLESS_2_TARGET.md) and ordered by the
[execution plan](../exec-plans/active/seamless-2.md).

Priority means consequence if left unresolved: High threatens privacy, lifetime
correctness or release integrity; Medium affects reliability, maintenance or
confidence. R01/R02 closed in Phase 1 and R11 closed during Phase 2; the
remaining entries are open. A local containment does not close a risk.

| ID / priority | Evidence and consequence | Present containment | Owning Phase / closure evidence |
| --- | --- | --- | --- |
| R01 High: mutable release tags — closed in Phase 1 | At the Phase 0 freeze, scripts/publish.ps1 used tag -f and a forced tag push; its test expected tag movement. | Publisher now rejects existing local/remote tags before committing and atomically pushes main with a new tag without force. | Phase 1: regression failed against the old publisher and passes after correction; disposable-remote test also rejects a tag ref and verifies that remote main does not advance. Real publication was not attempted. |
| R02 High: publication privacy filter — closed in Phase 1 | At the Phase 0 freeze, Test-PublicSourcePath omitted scrcpy-settings.json under an allowed directory. No data publication was observed. | Publisher rejects force-staged private settings and scans every outgoing commit for excluded paths, including an already committed private file. | Phase 1: force-staged and unpushed-commit privacy regressions pass after correction; public root AGENTS.md is admitted. |
| R03 High: shared ADB trust | Reviewed ADB 34.0.5 uses the Windows profile .android key directory. A separate port/environment override does not establish isolated keys. Windows ADB may spawn a persistent shared daemon as a descendant of a command, so tree termination on cancellation could stop a daemon owned by other tools. | Owner approved shared ADB trust/server for DEV; files/settings/shortcuts/mutex remain separate. Phase 3 command cancellation now kills only its direct process, with a regression proving a descendant survives; no `adb kill-server` cleanup. No personal keys/config are copied. | 2/3: DEV isolation and command ownership defined/tested. Shared-device/daemon effects and stronger isolation claims remain open. |
| R04 Medium: installation boundaries | Default launcher mutex is shared across installs; shortcut description matching can update another installation's shortcut. | DEV staging uses a separate mutex and package-private shortcuts. Phase 3 defines a same-user control-center activation contract and multiple-session registry, but has not wired the Windows channel or shortcut handling. | 5: implement and test the actual same-user activation channel, installation identity and shortcut ownership across two installations. |
| R05 High: native generation/lifetime safety | Static scrcpy aggregate, init flags, retained screen controller pointers and global callback gate rely on join/drain/flush ordering. No generation rejection exists. No new UAF was proven. | No production rewrite in Phase 0; record exact ownership and teardown ordering. | 7: explicit app/session owners, detached input, generation-aware dispatcher and deterministic late-work/teardown tests. |
| R06 High: teardown responsiveness | Main-thread synchronous joins can block despite responsive reconnect-wait loops. Current tests do not cover disconnect during every shutdown stage. | Do not claim move/resize/close responsiveness for all loss scenarios. | 7/8: bounded cancellation, controllable blocked workers, window/Stop tests during teardown and retries. |
| R07 Medium: reconnect product limits | Visible playback only; recording/positive deadline/headless conflicts, fixed retry delay, no general retry classification/budget or automatic USB failback. | New tests characterize current restrictions, explicitly replaceable in Phase 8. | 8: ConnectionManager policy, segmented recording, overall deadline, meaningful headless behavior, degradation and hysteresis tests. |
| R08 Medium: launcher/persistence coupling | Configuration lock spans final validation and process creation; mutable hashtable snapshots and HWND/environment contracts are legacy boundaries. The legacy launcher can still edit v1 after v2 migration, so a future combined flow needs an explicit boundary. | Existing snapshot, cancellation and process ownership tests remain authoritative for 1.x. Phase 3 makes valid v2 canonical for the new architecture, never auto-reimports v1 over it, reports corrupt v2 for recovery, and tests that cutover. The running 1.x launcher is not replaced. | 5: isolate legacy compatibility when new Desktop becomes active; no dual canonical writers or automatic two-way synchronization. 6: replace legacy HWND/environment lifetime contracts with owned IPC/process behavior. |
| R09 Medium: metadata/protocol drift | Options are duplicated across native/catalog/UI; device protocol has local vectors but no complete cross-language contract/fuzz suite. Phase 2 ran 47 Android server unit tests, which do not close that protocol gap. | Existing parity tests and exact reviewed server retained. | 4/6/9: canonical basic option metadata, named complex rules, framed IPC and device-protocol golden/malformed vectors. |
| R10 High: incomplete source-build chain | The 1.x package still imports its reviewed server and runtime libraries; no unified lock across every toolchain, SBOM or full reproducibility evidence exists. | Phase 2 built the Android server and native client with pinned top-level tools while preserving reviewed runtime hashes and the imported fallback. Android Gradle has separate buildscript/project locks and SHA-256 artifact verification; a clean-cache strict build and hosted safeguards passed. The source-built server worked in one [DEV-only hardware smoke](../development/phase2-source-server-smoke.md), but is not bundled in the canonical 1.x package. Initial repository trust and other toolchain provenance remain bounded claims. | 12/13: dependency-source obligations, SBOM, provenance and measured reproducibility where claimed. |
| R11 Medium: fingerprint input scope — closed in Phase 2 | The old native fingerprint included ignored generated Gradle files, making a valid built client stale after server builds. | Git-tracked and nonignored new native-tree inputs are now fingerprinted; ignored outputs are excluded without bypassing stale-source checks. | Phase 2: regression failed before the change, passed afterward, and real Gradle output did not change the native fingerprint. |
| R12 High: validation coverage | C unit tests do not form a reconnect harness. One Phase 2 smoke restored video/control but lost PC audio after USB-to-Wi-Fi recovery; the playback source was not recorded. Controlled A/B then restored audio 3/3 on the Phase 0 build and 3/3 on the Phase 2 build. The single failure remains unexplained; neither a Phase 2 regression nor a pre-existing 1.x bug is proven, and rare intermittent failure remains possible. Sample-drop messages are not established as its cause. | The owner accepted classification C and made this a non-blocking Phase 2 watch item, not a fixed bug. Preserve the [A/B evidence](../development/phase2-audio-ab.md). All four hosted PR #3 jobs passed after Gradle hardening. One [source-built-server DEV cycle](../development/phase2-source-server-smoke.md) restored video, control and PC audio with stable native PID/HWND; this does not prove rare failure absent. Current legacy diagnostics cannot separate source content, demux/decode, buffering and Windows output state. | Phase 6: structured IPC/lifecycle correlation. Phase 7: session/audio lifetime observability. Phase 8: reconnect/audio recovery semantics. Phase 12: hardware/soak audio recovery stress and aggregate diagnostics. |
| R13 Medium: documentation/distribution drift | Phase 1 corrected the FFmpeg version, old repository URLs and publisher root AGENTS.md allowlist. The legacy packager still copies repository-oriented `docs/` recursively; nested links to omitted source/scripts may not resolve in the portable ZIP. | Phase 2 retains that compatible package layout and validates source links, root package links, package structure and privacy. It does not claim every nested packaged development document is self-contained; see [packaging provenance](../PACKAGING.md). | 12/13 before a public 2.0 distribution: define the portable documentation set and validate all links within the archive. This tracked distribution risk does not block Phase 3 foundation work. |
| R14 Medium: reuse/license evidence | Existing notices describe LGPL dependencies and a source companion; companion availability/transitive compliance were not established. Future competitors have not been audited. | No copied competitor code/assets, no public DEV release or compliance attestation. | 1/10/12: exact-source reuse records and licensing decisions; verify notices/source offer and distribution obligations before release. |
| R15 Medium: lifecycle observability deferred | The legacy DEV trace retained PID/HWND but native messages arrived in buffered batches and could not align transport events with perceived audio outcomes. Phase 0 and Phase 2 sample-drop messages are observations, not a proven cause of audible failure. Building diagnostics only after the 2.0 runtime would leave the new event contract and session identities untestable during migration. | Keep the local observer for legacy diagnosis; do not claim it supplies native event times or change production code for the unproven audio symptom. | 6: ordered typed IPC/lifecycle events and immediate critical flush. 7: generation-aware capture/demux/decode/regulator/SDL-sink lifetime. 8: transport and first audio packet/frame after reconnect with channel-specific readiness. 12: private JSONL bundles, aggregated drop/underflow/overflow metrics, Windows output state, rotation and hardware/soak correlation. |

## Acceptance and review

The owner accepted and closed Phase 0 on 2026-09-24 with its documented hardware
and trace-timing limits. Phase 1 closed the specific R01/R02 publisher gaps;
that does not certify reconnect reliability, complete isolation of raw ADB
invocations, full license compliance, or release readiness.

Close a row only with linked implementation, tests and any required hardware or
release evidence. Reassess severity when the owning Phase discovers new facts.
Do not combine a narrow upstream correctness fix with a broad lifetime rewrite.
Phase 1 changed release tooling and documentation only. Phase 2 ports two
isolated upstream native correctness fixes. The renewed physical-device A/B
in R12 supplements their automated tests but does not prove rare audio failure
absent or establish a complete reconnect harness.
